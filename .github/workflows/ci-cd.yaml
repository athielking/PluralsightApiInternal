name: Pluralsight CICD  

on:
  workflow_dispatch:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']
    types: ['opened', 'synchronize']

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
    
    - name: Restore Dependencies
      run: dotnet restore

    - id: build
      name: Build      
      run: dotnet build --no-restore

    - run: ls PluralsightApi.Web 

    - id: test
      name: Test
      run: dotnet test "PluralsightApi.Tests/PluralsightApi.Tests.csproj" --collect "XPlat Code Coverage" -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=cobertura

    - id: coverage
      name: Code Coverage Report
      uses: irongut/CodeCoverageSummary@v1.3.0
      with:
        filename: PluralsightApi.Tests/TestResults/**/coverage.cobertura.xml
        badge: false
        fail_below_min: false        
        hide_branch_rate: false
        hide_complexity: true
        indicators: true
        output: console
    
    - id: publish
      name: Publish
      run: dotnet publish "PluralsightApi.Web/PluralsightApi.Web.csproj" -c Release -o ./publish --self-contained -r linux-x64 /p:UseAppHost=true

    - name: Zip Artifact
      run: |
        cd ./publish 
        zip -r ../app.zip .

    - id: upload
      name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: dotnet-webapp
        path: app.zip
  deploy:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
    - name: Download Artifact
      uses: actions/download-artifact@v8
      with:
        name: dotnet-webapp
    
    - name: Deploy to Azure
      uses: azure/webapps-deploy@v2
      with:
        app-name: pluralsight-api
        package: app.zip  
